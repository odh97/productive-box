name: Update gist
on:
  push:
    branches:
      - master
  schedule:
    - cron: "0 0 * * *"
jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - name: Update gist
        uses: maxam2017/productive-box@master
        env:
          GH_TOKEN: ${{ secrets.xzdheogus1 }}
          GIST_ID: ${{ secrets.5026884d75cfeaadb2f27922be8a7e33 }}
          TIMEZONE: Asia/Seoul
